databaseChangeLog:
  - changeSet:
      id: create-issuingbankschema
      author: yourname
      preConditions:
        onFail: CONTINUE
          - sqlCheck:
              expectedResult: 0
              sql: SELECT count(*) FROM information_schema.schemata WHERE schema_name = 'issuingbankschema'
      changes:
        - sql:
            sql: CREATE SCHEMA IF NOT EXISTS issuingbankschema
